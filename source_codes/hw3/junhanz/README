The passes can all be built by just running the Makefile.
To run the LICM and DeadCodeElimination passes the passes in code have to gone through the mem2reg path.
The LICM path includes the dominator path so when it is run, the results of the dominator path would be printed to console.
